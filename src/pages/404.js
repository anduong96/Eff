import React from 'react'
import { Layout } from 'antd'
import '../css/main.css'

export default () => (
    <div id="app-main">
        <Layout>
            <Layout.Content className={'dead-center'}>
                Error
            </Layout.Content>
        </Layout>
    </div>
)
